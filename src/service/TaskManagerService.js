/* Maybe i can keep my todolist app mostly the same, just make sure to send
the todos to the database when they are created, and get them from the database 
when the app runss.

If an update is made to a todo or subtask in front end, that same change to should take place
in the database

form submit method needs to change between post and put depending on whether an edit or new task

*/

class TaskManagerService {
  // transfer data from endpoint to database and vice versa
}

export default TaskManagerService;
